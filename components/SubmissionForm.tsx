'use client';

/**
 * SubmissionForm - tidak digunakan (dead code)
 * Komponen ini tidak dirender di mana pun. Halaman utama menggunakan ExpenseForm.
 * Disederhanakan agar tidak menyebabkan build error.
 *
 * @deprecated Gunakan ExpenseForm dari components/ExpenseForm.tsx
 */

export function SubmissionForm() {
  return null;
}
